#include "main.h"

/**
 * add - returns the absolute value of number it takes
 * @a: the arguement number 1
 * @l: the arguement number 2
 * Return: add value
 */
int add(int a, int l)
{
int r = a + l;
return (r);
}
